# 邮件发送配置
# ================

# SMTP服务器设置（默认使用Gmail）
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# 发件人邮箱
EMAIL_SENDER=your-email@gmail.com

# 发件人密码
# Gmail用户请使用"应用专用密码"，不是邮箱登录密码
# 获取方式：
# 1. 登录Google账号 -> 安全性 -> 两步验证（需先开启）
# 2. 两步验证页面底部 -> 应用专用密码
# 3. 选择"邮件"和"Mac"，生成16位密码
# 4. 将生成的密码填入下方（无空格）
EMAIL_PASSWORD=xxxx-xxxx-xxxx-xxxx

# 收件人邮箱（可以和发件人相同）
EMAIL_RECEIVER=your-email@gmail.com
